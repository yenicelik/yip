# yip
A safe and fast content-aware URL shortener. Yip is great

Be sure to re-download the nltk_data and nltk within the 'include' folder if you want to reuse it (standard nltk-module download).

Any property set to 'APIKEY' must be changed to the respective API-Key.